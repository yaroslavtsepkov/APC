# APC
Applied Parallel Computing

**Ready programms**

- Pi calc (CPU&GPU)

- Spectogram (CPU&GPU)

- MatrixMult (CPU&GPU)

- GA (CPU&GPU(TORCH))

- BF (CPU only, GPU is coming..)

- Salt&Pepper(CPU only, GPU is coming..)
